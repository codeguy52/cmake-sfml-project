name: CI

on: [push, pull_request]

jobs:
  build:
    name: Typecheck, test and build
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Node
        uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm

      - name: Install dependencies
        run: npm ci

      - name: Typecheck
        run: npm run typecheck

      - name: Test
        run: npm test

      # Also exercises scripts/vendor-ocr-assets.mjs via the prebuild hook, so a
      # missing OCR asset fails here rather than in someone's browser.
      - name: Build
        run: npm run build

      # The linking backend is real code that the app build never touches. Its
      # routing is a plain Request -> Response function, so most of it is
      # covered without a port at all.
      - name: Test the linking backend
        working-directory: server
        run: |
          npm ci
          npm test

      # Then boot it for real, which is the only way to exercise the node:http
      # adapter the tests deliberately bypass.
      - name: Smoke-test the linking backend over HTTP
        working-directory: server
        run: |
          PROVIDER=mock-personal ALLOWED_ORIGINS=http://localhost node src/server.js &
          for i in $(seq 1 20); do
            sleep 0.5
            if curl -sf -X POST http://127.0.0.1:8787/api/link/health \
                 -H 'Content-Type: application/json' -d '{}' -o health.json; then
              break
            fi
          done
          cat health.json
          grep -q '"mode":"personal"' health.json
          curl -sf -X POST http://127.0.0.1:8787/api/link/holdings \
            -H 'Content-Type: application/json' -d '{}' | grep -q 'snapshots'
